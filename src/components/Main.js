import React from "react";
import { CreateVault } from "./CreateVault";
import { FirstTime } from "./FirstTime";

export const Main = () => {

  return (
    <main>
        <CreateVault />
    </main>
  );
};